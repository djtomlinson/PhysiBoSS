PYTHON README:
	2 python codes are provided for generating cells in 2D (no z coordinate) and 3D.
	this generates a cells.csv
	place this in ./config
	a cells.csv already exists in that folder which works (check for comparison).
